extends ../layouts/common.pug 
block content  
    h1 Create user

    if errors 
        each error in errors 
            .alert.alert-danger= error 

    form(action="/users/create", method = "POST")
        .form-group 
            label.p-2(for="name") Name 
            input#name(type="text", name="name" class="form-control", value=values ?
            values.name : '') 
            //value bằng values nếu ko có values thì trả về rỗng ( '' )

        .form-group 
            label.p-2(for="phone") Phone
            input#phone(type="text", name="phone" class="form-control", alue=values ?
            values.phone : '')

        button.btn.btn-primary.m-2  Create 
